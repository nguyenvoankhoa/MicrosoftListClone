package com.repository;

import com.model.ColumnConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ColumnConfigRepository extends JpaRepository<ColumnConfig, String> {
}
